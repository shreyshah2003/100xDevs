<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landing Page</title>

    <style>
        /* ---------- Global Reset ---------- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            background-color: #f4f6f8;
            color: #333;
            line-height: 1.6;
        }

        /* ---------- Header ---------- */
        header {
            background: #0a2540;
            color: #ffffff;
            padding: 60px 20px;
            text-align: center;
        }

        header h1 {
            font-size: 2.8rem;
            margin-bottom: 15px;
        }

        header p {
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto 30px;
        }

        header a {
            display: inline-block;
            text-decoration: none;
            background: #4f46e5;
            color: #ffffff;
            padding: 12px 30px;
            border-radius: 5px;
            font-weight: bold;
        }

        header a:hover {
            background: #4338ca;
        }

        /* ---------- Section ---------- */
        section {
            padding: 60px 20px;
            max-width: 1100px;
            margin: auto;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .card {
            background: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.08);
            text-align: center;
        }

        .card h3 {
            margin-bottom: 10px;
            color: #0a2540;
        }

        .card p {
            font-size: 0.95rem;
        }

        /* ---------- Footer ---------- */
        footer {
            background: #0a2540;
            color: #ffffff;
            text-align: center;
            padding: 20px;
            font-size: 0.9rem;
        }

        footer span {
            opacity: 0.8;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header>
        <h1>Your Product Name</h1>
        <p>
            A simple and modern landing page built using only HTML and CSS.
            Perfect for quick demos and prototypes.
        </p>
        <a href="#">Get Started</a>
    </header>

    <!-- Features Section -->
    <section>
        <div class="features">
            <div class="card">
                <h3>Fast Setup</h3>
                <p>Single-file structure that runs instantly in any browser.</p>
            </div>

            <div class="card">
                <h3>Clean Design</h3>
                <p>Minimal UI focused on clarity and user experience.</p>
            </div>

            <div class="card">
                <h3>Fully Responsive</h3>
                <p>Works seamlessly across desktops, tablets, and mobiles.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <span>© 2026 Your Company Name. All rights reserved.</span>
    </footer>

</body>
</html>
